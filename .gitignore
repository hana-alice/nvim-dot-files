tt.*
.tests
doc/tags
debug
.repro
foo.*
*.log
data
.omx/
.tmp/
__pycache__/
*.pyc

.hermes/
tmp/

# Claude Code runtime cache (CLI version copies / locks / staging) — never a project asset
claude/
# Machine-local Claude permission state (not for the public mirror)
.claude/settings.local.json
