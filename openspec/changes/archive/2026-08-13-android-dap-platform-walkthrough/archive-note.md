# Archive note

Archived without syncing the delta spec. The proposed pure-listen gdbserver /
structured `gdb-remote-port` route was superseded by the device-verified
remote-android platform-mode route documented in `android-dap-attach` and
`docs/CONSTRAINTS.md` K30–K37. The unchecked tasks are intentionally preserved
as rejected investigation history, not represented as completed work.
